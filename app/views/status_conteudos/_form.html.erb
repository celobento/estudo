<%= form_for(@status_conteudo) do |f| %>
  <% if @status_conteudo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status_conteudo.errors.count, "error") %> prohibited this status_conteudo from being saved:</h2>

      <ul>
      <% @status_conteudo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
